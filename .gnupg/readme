Solución gpg:
importar clave privada
gpg --import <pkey>
editar trust
gpg --edit-key <pkey> # trust # 5

echo "no-allow-external-cache" >> ~/.gnupg/gpg-agent.conf

gpg-connect-agent reloadagent /bye

install gtk2
