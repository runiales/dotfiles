#!/bin/sh

# ORÍGENES a copiar

todo="

"

trabajo="

"

tareas="

"

claves="

"

# DESTINO a elegir

destino="
sj	78.30.56.121:
db	192.168.1.140:
lab	192.168.1.123:
disco	/mnt/Disco\ Duro/
lápiz	/mnt/Lápiz/
"

echo "$destino" | grep -P "^$(echo "$destino" | grep "https:" | sed 's/\t.*//g' | dmenu -i -p "Learn about what? (ESC to cancel)" -l 20 | awk '{print $1}')\s" | sed 's/.*\t//' | xargs -r mpv
echo "$destino" | grep -P "^$(echo "$destino" | grep "https:" | sed 's/\t.*//g' | dmenu -i -p "Learn about what? (ESC to cancel)" -l 20 | awk '{print $1}')\s" | sed 's/.*\t//' | rsync -aP
